--- a/include/qd/dd_real.h
+++ b/include/qd/dd_real.h
@@ -283,6 +283,90 @@
 QD_API bool operator!=(const dd_real &a, const td_real &b);
 QD_API bool operator!=(const td_real &a, const dd_real &b);
 
+template <typename Integer>
+struct dd_real_integer_operand
+    : std::integral_constant<bool,
+          std::is_same<Integer, int>::value ||
+          std::is_same<Integer, unsigned int>::value ||
+          std::is_same<Integer, long>::value ||
+          std::is_same<Integer, unsigned long>::value ||
+          std::is_same<Integer, long long>::value ||
+          std::is_same<Integer, unsigned long long>::value ||
+          std::is_same<Integer, std::int64_t>::value ||
+          std::is_same<Integer, std::uint64_t>::value> {};
+
+template <typename Integer,
+          typename std::enable_if<dd_real_integer_operand<Integer>::value, int>::type = 0>
+inline bool operator==(const dd_real &a, Integer b) {
+  return a == dd_real(b);
+}
+
+template <typename Integer,
+          typename std::enable_if<dd_real_integer_operand<Integer>::value, int>::type = 0>
+inline bool operator==(Integer a, const dd_real &b) {
+  return dd_real(a) == b;
+}
+
+template <typename Integer,
+          typename std::enable_if<dd_real_integer_operand<Integer>::value, int>::type = 0>
+inline bool operator!=(const dd_real &a, Integer b) {
+  return a != dd_real(b);
+}
+
+template <typename Integer,
+          typename std::enable_if<dd_real_integer_operand<Integer>::value, int>::type = 0>
+inline bool operator!=(Integer a, const dd_real &b) {
+  return dd_real(a) != b;
+}
+
+template <typename Integer,
+          typename std::enable_if<dd_real_integer_operand<Integer>::value, int>::type = 0>
+inline bool operator<(const dd_real &a, Integer b) {
+  return a < dd_real(b);
+}
+
+template <typename Integer,
+          typename std::enable_if<dd_real_integer_operand<Integer>::value, int>::type = 0>
+inline bool operator<(Integer a, const dd_real &b) {
+  return dd_real(a) < b;
+}
+
+template <typename Integer,
+          typename std::enable_if<dd_real_integer_operand<Integer>::value, int>::type = 0>
+inline bool operator>(const dd_real &a, Integer b) {
+  return a > dd_real(b);
+}
+
+template <typename Integer,
+          typename std::enable_if<dd_real_integer_operand<Integer>::value, int>::type = 0>
+inline bool operator>(Integer a, const dd_real &b) {
+  return dd_real(a) > b;
+}
+
+template <typename Integer,
+          typename std::enable_if<dd_real_integer_operand<Integer>::value, int>::type = 0>
+inline bool operator<=(const dd_real &a, Integer b) {
+  return a <= dd_real(b);
+}
+
+template <typename Integer,
+          typename std::enable_if<dd_real_integer_operand<Integer>::value, int>::type = 0>
+inline bool operator<=(Integer a, const dd_real &b) {
+  return dd_real(a) <= b;
+}
+
+template <typename Integer,
+          typename std::enable_if<dd_real_integer_operand<Integer>::value, int>::type = 0>
+inline bool operator>=(const dd_real &a, Integer b) {
+  return a >= dd_real(b);
+}
+
+template <typename Integer,
+          typename std::enable_if<dd_real_integer_operand<Integer>::value, int>::type = 0>
+inline bool operator>=(Integer a, const dd_real &b) {
+  return dd_real(a) >= b;
+}
+
 QD_API dd_real nint(const dd_real &a);
 QD_API dd_real floor(const dd_real &a);
 QD_API dd_real ceil(const dd_real &a);
--- a/include/qd/qd_real.h
+++ b/include/qd/qd_real.h
@@ -284,6 +284,78 @@
 QD_API bool operator!=(double a, const qd_real &b);
 QD_API bool operator!=(const qd_real &a, double b);
 
+template <typename Integer,
+          typename std::enable_if<qd_real_integer_operand<Integer>::value, int>::type = 0>
+inline bool operator==(const qd_real &a, Integer b) {
+  return a == qd_real(b);
+}
+
+template <typename Integer,
+          typename std::enable_if<qd_real_integer_operand<Integer>::value, int>::type = 0>
+inline bool operator==(Integer a, const qd_real &b) {
+  return qd_real(a) == b;
+}
+
+template <typename Integer,
+          typename std::enable_if<qd_real_integer_operand<Integer>::value, int>::type = 0>
+inline bool operator!=(const qd_real &a, Integer b) {
+  return a != qd_real(b);
+}
+
+template <typename Integer,
+          typename std::enable_if<qd_real_integer_operand<Integer>::value, int>::type = 0>
+inline bool operator!=(Integer a, const qd_real &b) {
+  return qd_real(a) != b;
+}
+
+template <typename Integer,
+          typename std::enable_if<qd_real_integer_operand<Integer>::value, int>::type = 0>
+inline bool operator<(const qd_real &a, Integer b) {
+  return a < qd_real(b);
+}
+
+template <typename Integer,
+          typename std::enable_if<qd_real_integer_operand<Integer>::value, int>::type = 0>
+inline bool operator<(Integer a, const qd_real &b) {
+  return qd_real(a) < b;
+}
+
+template <typename Integer,
+          typename std::enable_if<qd_real_integer_operand<Integer>::value, int>::type = 0>
+inline bool operator>(const qd_real &a, Integer b) {
+  return a > qd_real(b);
+}
+
+template <typename Integer,
+          typename std::enable_if<qd_real_integer_operand<Integer>::value, int>::type = 0>
+inline bool operator>(Integer a, const qd_real &b) {
+  return qd_real(a) > b;
+}
+
+template <typename Integer,
+          typename std::enable_if<qd_real_integer_operand<Integer>::value, int>::type = 0>
+inline bool operator<=(const qd_real &a, Integer b) {
+  return a <= qd_real(b);
+}
+
+template <typename Integer,
+          typename std::enable_if<qd_real_integer_operand<Integer>::value, int>::type = 0>
+inline bool operator<=(Integer a, const qd_real &b) {
+  return qd_real(a) <= b;
+}
+
+template <typename Integer,
+          typename std::enable_if<qd_real_integer_operand<Integer>::value, int>::type = 0>
+inline bool operator>=(const qd_real &a, Integer b) {
+  return a >= qd_real(b);
+}
+
+template <typename Integer,
+          typename std::enable_if<qd_real_integer_operand<Integer>::value, int>::type = 0>
+inline bool operator>=(Integer a, const qd_real &b) {
+  return qd_real(a) >= b;
+}
+
 QD_API qd_real fabs(const qd_real &a);
 QD_API qd_real abs(const qd_real &a);    /* same as fabs */
 
--- a/include/qd/td_real.h
+++ b/include/qd/td_real.h
@@ -305,6 +305,90 @@
 QD_API bool operator>=(const qd_real &a, const td_real &b);
 QD_API bool operator>=(const td_real &a, const td_real &b);
 
+template <typename Integer>
+struct td_real_integer_operand
+    : std::integral_constant<bool,
+          std::is_same<Integer, int>::value ||
+          std::is_same<Integer, unsigned int>::value ||
+          std::is_same<Integer, long>::value ||
+          std::is_same<Integer, unsigned long>::value ||
+          std::is_same<Integer, long long>::value ||
+          std::is_same<Integer, unsigned long long>::value ||
+          std::is_same<Integer, std::int64_t>::value ||
+          std::is_same<Integer, std::uint64_t>::value> {};
+
+template <typename Integer,
+          typename std::enable_if<td_real_integer_operand<Integer>::value, int>::type = 0>
+inline bool operator==(const td_real &a, Integer b) {
+  return a == td_real(b);
+}
+
+template <typename Integer,
+          typename std::enable_if<td_real_integer_operand<Integer>::value, int>::type = 0>
+inline bool operator==(Integer a, const td_real &b) {
+  return td_real(a) == b;
+}
+
+template <typename Integer,
+          typename std::enable_if<td_real_integer_operand<Integer>::value, int>::type = 0>
+inline bool operator!=(const td_real &a, Integer b) {
+  return a != td_real(b);
+}
+
+template <typename Integer,
+          typename std::enable_if<td_real_integer_operand<Integer>::value, int>::type = 0>
+inline bool operator!=(Integer a, const td_real &b) {
+  return td_real(a) != b;
+}
+
+template <typename Integer,
+          typename std::enable_if<td_real_integer_operand<Integer>::value, int>::type = 0>
+inline bool operator<(const td_real &a, Integer b) {
+  return a < td_real(b);
+}
+
+template <typename Integer,
+          typename std::enable_if<td_real_integer_operand<Integer>::value, int>::type = 0>
+inline bool operator<(Integer a, const td_real &b) {
+  return td_real(a) < b;
+}
+
+template <typename Integer,
+          typename std::enable_if<td_real_integer_operand<Integer>::value, int>::type = 0>
+inline bool operator>(const td_real &a, Integer b) {
+  return a > td_real(b);
+}
+
+template <typename Integer,
+          typename std::enable_if<td_real_integer_operand<Integer>::value, int>::type = 0>
+inline bool operator>(Integer a, const td_real &b) {
+  return td_real(a) > b;
+}
+
+template <typename Integer,
+          typename std::enable_if<td_real_integer_operand<Integer>::value, int>::type = 0>
+inline bool operator<=(const td_real &a, Integer b) {
+  return a <= td_real(b);
+}
+
+template <typename Integer,
+          typename std::enable_if<td_real_integer_operand<Integer>::value, int>::type = 0>
+inline bool operator<=(Integer a, const td_real &b) {
+  return td_real(a) <= b;
+}
+
+template <typename Integer,
+          typename std::enable_if<td_real_integer_operand<Integer>::value, int>::type = 0>
+inline bool operator>=(const td_real &a, Integer b) {
+  return a >= td_real(b);
+}
+
+template <typename Integer,
+          typename std::enable_if<td_real_integer_operand<Integer>::value, int>::type = 0>
+inline bool operator>=(Integer a, const td_real &b) {
+  return td_real(a) >= b;
+}
+
 QD_API dd_real to_dd_real(const td_real &a);
 QD_API td_real to_td_real(const dd_real &a);
 QD_API td_real to_td_real(const qd_real &a);
--- a/include/qd/edd_real.h
+++ b/include/qd/edd_real.h
@@ -254,6 +254,90 @@
 QD_API bool operator>=(edd_word a, const edd_real &b);
 QD_API bool operator>=(const edd_real &a, const edd_real &b);
 
+template <typename Integer>
+struct edd_real_integer_operand
+    : std::integral_constant<bool,
+          std::is_same<Integer, int>::value ||
+          std::is_same<Integer, unsigned int>::value ||
+          std::is_same<Integer, long>::value ||
+          std::is_same<Integer, unsigned long>::value ||
+          std::is_same<Integer, long long>::value ||
+          std::is_same<Integer, unsigned long long>::value ||
+          std::is_same<Integer, std::int64_t>::value ||
+          std::is_same<Integer, std::uint64_t>::value> {};
+
+template <typename Integer,
+          typename std::enable_if<edd_real_integer_operand<Integer>::value, int>::type = 0>
+inline bool operator==(const edd_real &a, Integer b) {
+  return a == edd_real(b);
+}
+
+template <typename Integer,
+          typename std::enable_if<edd_real_integer_operand<Integer>::value, int>::type = 0>
+inline bool operator==(Integer a, const edd_real &b) {
+  return edd_real(a) == b;
+}
+
+template <typename Integer,
+          typename std::enable_if<edd_real_integer_operand<Integer>::value, int>::type = 0>
+inline bool operator!=(const edd_real &a, Integer b) {
+  return a != edd_real(b);
+}
+
+template <typename Integer,
+          typename std::enable_if<edd_real_integer_operand<Integer>::value, int>::type = 0>
+inline bool operator!=(Integer a, const edd_real &b) {
+  return edd_real(a) != b;
+}
+
+template <typename Integer,
+          typename std::enable_if<edd_real_integer_operand<Integer>::value, int>::type = 0>
+inline bool operator<(const edd_real &a, Integer b) {
+  return a < edd_real(b);
+}
+
+template <typename Integer,
+          typename std::enable_if<edd_real_integer_operand<Integer>::value, int>::type = 0>
+inline bool operator<(Integer a, const edd_real &b) {
+  return edd_real(a) < b;
+}
+
+template <typename Integer,
+          typename std::enable_if<edd_real_integer_operand<Integer>::value, int>::type = 0>
+inline bool operator>(const edd_real &a, Integer b) {
+  return a > edd_real(b);
+}
+
+template <typename Integer,
+          typename std::enable_if<edd_real_integer_operand<Integer>::value, int>::type = 0>
+inline bool operator>(Integer a, const edd_real &b) {
+  return edd_real(a) > b;
+}
+
+template <typename Integer,
+          typename std::enable_if<edd_real_integer_operand<Integer>::value, int>::type = 0>
+inline bool operator<=(const edd_real &a, Integer b) {
+  return a <= edd_real(b);
+}
+
+template <typename Integer,
+          typename std::enable_if<edd_real_integer_operand<Integer>::value, int>::type = 0>
+inline bool operator<=(Integer a, const edd_real &b) {
+  return edd_real(a) <= b;
+}
+
+template <typename Integer,
+          typename std::enable_if<edd_real_integer_operand<Integer>::value, int>::type = 0>
+inline bool operator>=(const edd_real &a, Integer b) {
+  return a >= edd_real(b);
+}
+
+template <typename Integer,
+          typename std::enable_if<edd_real_integer_operand<Integer>::value, int>::type = 0>
+inline bool operator>=(Integer a, const edd_real &b) {
+  return edd_real(a) >= b;
+}
+
 QD_API edd_word to_float64x(const edd_real &a);
 QD_API dd_real to_dd_real(const edd_real &a);
 QD_API edd_real to_edd_real(const dd_real &a);
