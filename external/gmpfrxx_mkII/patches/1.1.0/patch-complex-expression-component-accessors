--- a/include/gmpfrxx_mkII/detail/expr.hpp
+++ b/include/gmpfrxx_mkII/detail/expr.hpp
@@ -144,6 +144,18 @@
 
     Result eval() const { return Result(*this); }
 
+    template <typename R = Result>
+    auto real() const -> std::decay_t<decltype(std::declval<const R&>().real())>
+    {
+        return eval().real();
+    }
+
+    template <typename R = Result>
+    auto imag() const -> std::decay_t<decltype(std::declval<const R&>().imag())>
+    {
+        return eval().imag();
+    }
+
 private:
     Expr expr_;
 };
@@ -163,6 +175,18 @@
 
     Result eval() const { return Result(*this); }
 
+    template <typename R = Result>
+    auto real() const -> std::decay_t<decltype(std::declval<const R&>().real())>
+    {
+        return eval().real();
+    }
+
+    template <typename R = Result>
+    auto imag() const -> std::decay_t<decltype(std::declval<const R&>().imag())>
+    {
+        return eval().imag();
+    }
+
 private:
     Lhs lhs_;
     Rhs rhs_;
