--- mplapack_old/mplapack/reference/Rsyevx_2stage.cpp	2021-08-01 17:23:21.621787593 +0900
+++ mplapack/mplapack/reference/Rsyevx_2stage.cpp	2021-08-01 15:53:29.425285008 +0900
@@ -172,7 +172,7 @@
     smlnum = safmin / eps;
     bignum = one / smlnum;
     rmin = sqrt(smlnum);
-    rmax = min(sqrt(bignum), one / sqrt(sqrt(safmin)));
+    rmax = min(REAL(sqrt(bignum)), REAL(one / sqrt(sqrt(safmin))));
     //
     //     Scale matrix to allowable range, if necessary.
     //
