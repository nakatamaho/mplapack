--- mplapack_old/mplapack/reference/Rsyevr.cpp	2021-08-01 17:23:21.621787593 +0900
+++ mplapack/mplapack/reference/Rsyevr.cpp	2021-08-01 15:53:29.425285008 +0900
@@ -200,7 +200,7 @@
     smlnum = safmin / eps;
     bignum = one / smlnum;
     rmin = sqrt(smlnum);
-    rmax = min(sqrt(bignum), one / sqrt(sqrt(safmin)));
+    rmax = min(REAL(sqrt(bignum)), REAL(one / sqrt(sqrt(safmin))));
     //
     //     Scale matrix to allowable range, if necessary.
     //
