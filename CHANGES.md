# MPLAPACK 2.1.0 Release Notes

## Highlights

### Fable Standalone Component — Automated Fortran-to-C++ Conversion
MPLAPACK 2.1.0 introduces `fable/` as a top-level, standalone component of the repository.
[Fable](https://cci.lbl.gov/fable/) (Automatic Fortran to C++ conversion) and FEM (Fortran Emulator)
have been extensively modified for MPLAPACK use. The pipeline now automates generation of both
library routines and test programs from LAPACK 3.9.1 Fortran sources.

**Permanent design decisions:**

- **Library routines (BLAS/LAPACK):** Converted by Fable only. FEM is **not** used.
- **Test programs (EIG/LIN/MATGEN):** FEM is used for formatted I/O support.
- **152 small patches** under `fable/3.9.1/` handle cases that cannot be automated.
  Fully unautomatable routines are rewritten from scratch.
- **Public headers** for MPBLAS and MPLAPACK are now auto-generated by shell scripts.

To reproduce the full conversion pipeline:
```sh
~/mplapack/fable/go.sh          # library routines (programs, headers, patching)
~/mplapack/fable/go_testing.sh  # test programs (EIG/LIN/MATGEN, programs, headers, patching)
```

### Compiler/Toolchain Support Constraints ⚠️

| Compiler | binary128 | binary80 |
|---|---|---|
| GCC | ✅ Supported | ✅ (x86/x86\_64 only) |
| Intel oneAPI (icx/icpx) | ❌ Not supported | ❌ Not supported |
| Clang/LLVM | ❌ Not supported | ✅ Supported |

> **Intel oneAPI users:** Both `binary128` and `binary80` backends are unavailable.
> **Clang users:** `binary128` backend is unavailable. Use GCC for full precision support.

### Extended BLAS Routines Removed

Extended BLAS routines and all LAPACK routines depending on them have been removed from the library.
Users relying on these interfaces must migrate. See [MIGRATION.md](MIGRATION.md).

---

## Breaking Changes

- **Extended BLAS routines removed.** Dependent MPLAPACK routines also removed.
- **Intel oneAPI:** `--enable-binary128` and `--enable-binary80` are non-functional.
- **Clang:** `--enable-binary128` is non-functional.
- **DD/QD output precision reduced:** DD from 32→30 significant digits, QD from 64→61 digits,
  to eliminate spurious last-digit mismatches across MPFR versions.

See [MIGRATION.md](MIGRATION.md) for full migration instructions.

---

## Bug Fixes

### Eigenvalue / Eigensolver (EIG)

- **`Rdrvst2stg`: infinite loop and segfault fixed.**
  A FABLE conversion bug caused three `goto statement_700` jumps in the `Rsyevr(V,V)` test
  section (tests 76–78) to branch backward into the `Rspev` section instead of advancing to
  the next `iuplo` iteration. The resulting infinite loop exhausted resources and caused a
  segfault. Fixed by introducing `statement_1720` as the correct forward jump target.
  Reproducer: `xeigtstR_mpfr < se2.in` with N=3, JTYPE=10, ISEED=(47829,4273,63548,61012).

- **GMP eigenvalue bisection: iteration-count cap extended to GMP.**
  GMP's effectively unbounded exponent range causes astronomically large `itmax`/`maxitr`
  values from the log-based formula. The existing 100,000-cap guard (previously MPFR-only)
  now also covers GMP. Affected: `Rlarrb`, `Rlarrd`, `Rlarrk`, `Rstebz`.

- **GMP test thresholds raised in `Rchkee`.**
  GMP has lower effective accuracy than MPFR/DD/QD in certain eigensolver code paths.
  Adjustments (GMP-specific only; MPFR is unaffected):
  - Non-symmetric block driver: 3.0× → 4.0×
  - Symmetric driver: 1.5× → 2.0×

### Numerics / Arithmetic

- **`mpcomplex` multiplication assignment operator fixed** (PR #74, contributor: pcbcos).
- **`binary128` complex print helper:** `n` variable reused correctly for `quadmath_snprintf`.
- **`binary128`/`binary80` I/O:** explicit `+` sign enforced in `strfrom`-based output paths.
- **`long`/`unsigned long` I/O overloads** fixed for ILP32/LLP64 platforms to prevent
  32-bit truncation on Windows and Debian i386.

### Known Issues

- **`lin/dd`: elevated failure count on AArch64 (ARM) — suspected GCC bug.**
  On Raspberry Pi 4 (Cortex-A72, AArch64, Ubuntu 24.04, GCC 13.3.0), the `lin/dd` test
  suite shows 226 failures (0.03%) vs. the expected ≤ 12 (0.00%) on amd64. All other
  backends (`binary128`, `mpfr`, `qd`, etc.) and `eig/dd` on the same hardware are within
  normal range. The root cause is under investigation and is suspected to be a GCC bug on
  AArch64 affecting double-double arithmetic.
  See: https://github.com/nakatamaho/mplapack/issues/76

### Portability / Platform

- **`binary80`: `M_PIl` replaced with portable hex-float literal.**
  `M_PIl` is a non-standard extension absent from musl libc (Alpine Linux). Replaced with
  `0xc.90fdaa22168c235p-2L`, portable across glibc, musl, macOS, and MinGW.

- **Fable `execinfo.h` conditional via `__has_include`.**
  Alpine Linux (musl) does not provide `<execinfo.h>`. Backtrace support is now disabled
  automatically when the header is absent, without requiring `libexecinfo`.

- **`std::once_flag`/`call_once` replaced with `std::atomic<bool>`** for MinGW win32 thread
  model compatibility.

- **FPU precision mode** set before test code runs in QD/DD BLAS test binaries.

- **Alpine Docker image:** GNU sed added explicitly (BusyBox sed lacks the `e` command).

### Build System

- **`$(top_builddir)` → `$(top_srcdir)`** for `fix_dylib_macOS.sh` and `cpu_os_detection.sh`
  across all test `Makefile.am` files (VPATH build fix).

- **Jobserver propagation:** `+` prefix added to all `${MAKE}` invocations in external subdirs
  (gmp, mpfr, mpc, qd, lapack, openblas). Removes spurious `-j1` fallback warning.
  Explicit `MAKEFLAGS = -j 1` removed.

- **`distclean`/`distcheck` failures in external subdirs fixed.** Bare `clean:`/`distclean:`
  targets replaced with `clean-local:`/`distclean-local:` automake hooks in all external
  subdirectories (dlfcn-win32, gmp, lapack, mpc, mpfr, openblas, qd).

- **Wildcards in `EXTRA_DIST` replaced with explicit file lists** (automake does not expand
  glob patterns in `EXTRA_DIST` during clean builds).

- **`qd_extra_cxxflags` propagation fixed:** missing `AC_SUBST` in `configure.ac` caused the
  flags to never be substituted into any Makefile.

- **`gen_include_mplapack.sh`:** additional internal symbols filtered from public header
  aggregation.

- **`quadmath` flag substitution spacing** fixed in generated example Makefiles.

---

## New Features and Enhancements

### Fable & Code Generation

- `fable/` promoted to a top-level, standalone component.
- MPBLAS and MPLAPACK public headers auto-generated by shell scripts
  (`gen_include_mpblas.sh`, `gen_include_mplapack.sh`).
- Conversion covers BLAS/LAPACK library routines **and** test programs (EIG/LIN/MATGEN).
- 152 targeted patches under `fable/3.9.1/` for Fortran constructs not handled by Fable.

### Platform & Architecture

- **Debian i386 (ILP32) support:** `mplapackint` unified with platform `long`; ILP32/LLP64
  I/O overloads added; `Dockerfile_debian12_386_devel` added.
- **CUDA 13.1.1** branch build jobs added for Ubuntu 22.04/24.04 (amd64).
- **Rocky Linux 8/9** and **Alpine 3.19** (amd64, arm64) added to branch build matrix.
- **MinGW-w64** cross-compile jobs added (Ubuntu 22.04/24.04 hosts).
- **Debian 12** including `linux/mips64le` added alongside Debian 13.

### Test Infrastructure

- `summarize_mplapack_tests.py` added; executed automatically after `make check`.
- `cpu_os_detection.sh` for deterministic CI platform identifiers.
- `Rlaruv` test enhanced with Welford-based statistical sanity checks
  (mean, variance, min, max, chi-squared). Deterministic mode made platform-independent.
- `Rlamch` and `Rlaruv` reference outputs added/renamed per backend.
- Test outputs routed into `CPU-OS-Compiler` subdirectory for disambiguation.
- EIG/LIN backend checks parallelized with jobserver-safe recursive make.

### Build Scripts

- Tiered build/test scripts added and consolidated (see Platform Support Tiers below).
- macOS tier1: WORKDIR fixed to `$HOME/tmp/mplapack` for improved ccache hit rate,
  with atomic lock and stale-lock detection.
- `AX_MPLAPACK_CC_TAG` macro added for robust compiler tag identification in CI.
- `MPLAPACK_BINARY128_MODE_*` and `MPLAPACK_BINARY80_MODE_*` AM_CONDITIONALs added.
- `iMlaver` generation and VPATH-safe external extraction added to `configure.ac`.

### Benchmark

- Benchmark subtree flattened: `mpblas/` and `mplapack/` subdirs merged into `benchmark/`.
- `go.*.sh` generated from `go.*.sh.in` templates with two-phase substitution
  (build-time: `%%CC_TAG%%`, `%%CPU_OS_DETECTION_SH%%`; install-time: `%%PREFIX%%`).
- Warmup passes, statistical timing, CSV output, and dynamic symbol resolver support added.

---

## Dependency Updates

| Library | 2.0.1 | 2.1.0 | Notes |
|---|---|---|---|
| GMP | 6.2.1 | 6.3.0 | |
| MPFR | 4.1.0 | 4.2.2 | |
| MPC | 1.2.1 | 1.3.1 | |
| QD | 2.3.23 | 2.3.24 | |
| dlfcn-win32 | 1.3.1 | 1.4.2 | |
| OpenBLAS | 0.3.21 | 0.3.30 | |
| LAPACK | 3.9.1 | 3.9.1 (primary) | LAPACK 3.12.1 bundled for future migration; not yet used |

---

## Platform Support Tiers

| Tier | Guarantee | Platforms |
|---|---|---|
| **Tier 1** | Functionality guaranteed (build + full test suite pass) | macOS Intel Sonoma (amd64), Windows / MinGW-w64 (amd64), Ubuntu 22.04 (amd64), Ubuntu 24.04 (amd64) |
| **Tier 2** | Build guaranteed | Alpine Linux 3.19 (amd64, arm64), Rocky Linux 8 (amd64), Rocky Linux 9 (amd64), Debian 12 (amd64, arm64, i386, mips64le), Debian 13 (amd64), Ubuntu 22.04 (arm64), Ubuntu 24.04 (arm64) |
| **Tier 3** | Patches accepted; no CI coverage | Other platforms |

Tier 1/2 build scripts are located in `misc/`:
```
misc/buildtest_tier1_amd64_macOS.sh
misc/buildtest_tier1_amd64_mingw64.sh
misc/buildtest_tier1_amd64_ubuntu.sh
misc/buildtest_tier2_arm64_ubuntu.sh
misc/buildtest_tier2_i386_debian.sh
```

---

## Test Results Summary

All results show **0.00% failure rate**. Non-zero absolute failure counts are within expected
numerical tolerance thresholds and do not indicate algorithmic defects.

### LIN Tests (linear solver routines) — all tested platforms

| Precision | Total Tests | Failures | Fail% |
|---|---|---|---|
| binary128 | 865,775 | 12 | 0.00% |
| binary80 | 865,775 | 1 | 0.00% |
| dd | 865,775 | 12 | 0.00% |
| double | 865,775 | 1 | 0.00% |
| gmp | 865,775 | 1 | 0.00% |
| mpfr (binary128) | 865,775 | 12 | 0.00% |
| mpfr (binary64) | 865,775 | 4 | 0.00% |
| mpfr (default) | 865,775 | 12 | 0.00% |
| qd | 865,775 | 12 | 0.00% |

### EIG Tests (eigenvalue routines) — Ryzen Threadripper 3970X / Ubuntu 22.04 / GCC 11.4.0

| Precision | Total Tests | Failures | Fail% | Files w/ Fail | File Count |
|---|---|---|---|---|---|
| binary128 | 1,232,900 | 0 | 0.00% | 0 | 32 |
| binary80 | 1,232,900 | 0 | 0.00% | 0 | 32 |
| dd | 1,206,500 | 2 | 0.00% | 1 | 32 |
| double | 1,232,900 | 0 | 0.00% | 0 | 32 |
| gmp | 1,205,060 | 15 | 0.00% | 3 | 30 |
| mpfr (binary128) | 1,232,900 | 0 | 0.00% | 0 | 32 |
| mpfr (binary64) | 1,232,896 | 2 | 0.00% | 1 | 32 |
| mpfr (default) | 1,232,900 | 1 | 0.00% | 1 | 32 |
| qd | 1,206,486 | 30 | 0.00% | 2 | 32 |

Additional test results available in the repository for: Core i5-9600 / Ubuntu 22.04,
Core i5-8500B / macOS 15, Ryzen Threadripper 3970X / Ubuntu 24.04, Ryzen Threadripper 3970X /
Windows / GCC 10, Cortex-A72 / Ubuntu 24.04 (Raspberry Pi 4).
